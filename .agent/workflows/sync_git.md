---
description: Sync code changes to GitHub
---

1. Check git status
   `git status`

2. Add all changes
   // turbo
   `git add .`

3. Commit changes (Generate a concise message describing the changes)
   `git commit -m "Update code"`

4. Push to remote
   // turbo
   `git push`
