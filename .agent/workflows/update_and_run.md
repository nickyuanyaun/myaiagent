---
description: Update code from git, install dependencies, and restart the bot
---

1. Pull latest code
   `git pull`

2. Install dependencies
   `pip install -r requirements.txt`

3. Run the bot
   `python main.py`
